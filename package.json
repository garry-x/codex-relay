{
  "name": "codex-relay",
  "version": "0.4.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "check": "node --check codex-relay && node --check codex-relay-forward",
    "test": "node --test"
  }
}
